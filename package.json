{
  "name": "smart-green-corridor",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm --prefix frontend install --ignore-scripts && npm --prefix frontend run build",
    "lint": "npm run lint --prefix frontend",
    "preview": "npm run preview --prefix frontend",
    "start": "python app.py"
  }
}
